<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8" />
<title>Admin tools</title>
</head>
<body>
	<h1>Admin tools</h1>
	<p>These tools allow you check the current database items.</p>
	<h2>Events</h2>
	<% if (events.length) { %>
        <ul>
          <% events.forEach(function(item){ %>
            <li><a href="/events/<%= item._id %>"><%= item._id %></a></li>
          <% }) %>
        </ul>
    <% } %>
</body>
l