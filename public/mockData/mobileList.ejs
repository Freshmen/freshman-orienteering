

<section id="listContent">
       <nav id="contentList">
        <ul id="eventList">
          <% for(var i = 0; i<content.length; i++) {%>
            <li><a href="#"><%= content[i].title%></a></li>  
            <% if (content[i].hasOwnProperty('organizer')) {%>  
            	<span class="listSmall" id="organizer"><%= 'Organizer: ' + content[i].organizer%></span>
            <%}%>         
          <% } %>
        </ul>
      </nav>
 </section>